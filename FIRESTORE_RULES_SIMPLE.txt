rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Productos: Todos pueden leer, usuarios autenticados pueden escribir
    match /products/{product} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
